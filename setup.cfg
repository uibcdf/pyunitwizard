# Helper file to handle all configs

[coverage:run]
# .coveragerc to control coverage.py and pytest-cov
omit =
    # Omit the tests
    */tests/*
    # Omit generated versioneer
    pyunitwizard/_version.py

[versioneer]
# Automatic version numbering scheme
VCS = git
style = pep440
versionfile_source = pyunitwizard/_version.py
versionfile_build = pyunitwizard/_version.py
tag_prefix = ''

[aliases]
test = pytest
