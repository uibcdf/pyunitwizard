[build-system]
requires = ["setuptools>=68.0", "versioningit>=3.0"]
build-backend = "setuptools.build_meta"

# Self-descriptive entries which should always be present
# https://packaging.python.org/en/latest/specifications/declaring-project-metadata/
[project]
name = "pyunitwizard"
description = "Quantities and units assistant"
dynamic = ["version"]
readme = "README.md"
authors = [
    { name = "UIBCDF Lab", email = "uibcdf@gmail.com" }
]
license = { text = "MIT" }
# See https://pypi.org/classifiers/
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.11.0,<3.14.0"
# Declare any run-time dependencies that should be installed with the package.
dependencies = [
    "numpy",
    "pint",
    "smonitor>=0.14.0",
    "depdigest",
]

# Update the urls once the hosting is set up.
[project.urls]
"Source" = "https://github.com/uibcdf/pyunitwizard/"
"Documentation" = "https://uibcdf.org/pyunitwizard"

[project.optional-dependencies]
test = [
  "pytest>=6.1.2",
  "pytest-runner"
]

[tool.setuptools]
# This subkey is a beta stage development and keys may change in the future, see https://setuptools.pypa.io/en/latest/userguide/pyproject_config.html for more details
#
# As of version 0.971, mypy does not support type checking of installed zipped
# packages (because it does not actually import the Python packages).
# We declare the package not-zip-safe so that our type hints are also available
# when checking client code that uses our (installed) package.
# Ref:
# https://mypy.readthedocs.io/en/stable/installed_packages.html?highlight=zip#using-installed-packages-with-mypy-pep-561
zip-safe = false
# Let setuptools discover the package in the current directory,
# but be explicit about non-Python files.
# See also:
#   https://setuptools.pypa.io/en/latest/userguide/pyproject_config.html#setuptools-specific-configuration
# Note that behavior is currently evolving with respect to how to interpret the
# "data" and "tests" subdirectories. As of setuptools 63, both are automatically
# included if namespaces is true (default), even if the package is named explicitly
# (instead of using 'find'). With 'find', the 'tests' subpackage is discovered
# recursively because of its __init__.py file, but the data subdirectory is excluded
# with include-package-data = false and namespaces = false.
include-package-data = false
[tool.setuptools.packages.find]
namespaces = false
where = ["."]
include = ["pyunitwizard*"]

# Ref https://setuptools.pypa.io/en/latest/userguide/datafiles.html#package-data
[tool.setuptools.package-data]
pyunitwizard = [
    "py.typed"
]

[tool.ruff]
target-version = "py311"
line-length = 119
include = ["*.py", "*.pyi"]
extend-exclude = [
    "DEPDIGEST_GUIDE.md",
    "GH_RUN_RECEPTOR_GUIDE.md",
    "MOLSYSSUITE_GUIDE.md",
    "SMONITOR_GUIDE.md",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]
"examples/testlib2/__init__.py" = ["I001"]
"pyunitwizard/__init__.py" = ["E402"]
"pyunitwizard/_smonitor.py" = ["E402"]
"tests/astropy_units/test_astropy_units.py" = ["E402"]
"tests/forms/test_api_physipy_and_quantities.py" = ["E402"]
"tests/integration/test_frontend_cross_backend_matrix.py" = ["E402"]
"tests/physipy/test_convert_physipy.py" = ["E402"]
"tests/test_get.py" = ["E402"]
"tests/utils/matplotlib/test_complex_layouts.py" = ["E402"]
"tests/utils/matplotlib/test_internal_helpers.py" = ["E402"]
"tests/utils/matplotlib/test_units_bridge.py" = ["E402"]

[tool.versioningit]
default-version = "0+unknown"

[tool.versioningit.format]
distance = "{base_version}+{distance}.{vcs}{rev}"
dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"
distance-dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"

[tool.versioningit.vcs]
# The method key:
method = "git"  # <- The method name
# Parameters to pass to the method:
match = ["*"]
default-tag = "0.17.0"

[tool.versioningit.tag2version]
regex = '^(?P<version>(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*))$'
require-match = true

[tool.versioningit.write]
file = "pyunitwizard/_version.py"

[tool.flit.external-data]
directory = "data"
