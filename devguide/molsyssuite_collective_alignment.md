# Alignment with `molsyssuite` Collective V1 Checklist

This document maps PyUnitWizard progress against:
`../molsyssuite/devguide/collective_v1_checklist.md`.

Scope:
- report PyUnitWizard local status with concrete evidence,
- keep collective validation explicitly separate,
- avoid marking collective requirements as completed from local-only evidence.

Status vocabulary:
- `done locally`
- `in progress`
- `blocked`
- `pending`

## 1. Cross-library standards

Status note (2026-03-03):
- smonitor: pending
- depdigest: pending
- argdigest: done locally (`0.9.0` RC; reference in molsyssuite checklist)
- pyunitwizard: done locally (see evidence below)
- collective validation: pending

PyUnitWizard local evidence:
- Unified configuration precedence (`runtime > env > file`) validated in
  `tests/test_configure.py`.
- Native instrumentation of critical public APIs via `@smonitor.signal`
  verified by inspection and checklist evidence.
- Optional dependencies governed through `_depdigest.py` contract, with tests in
  `tests/test_depdigest_contract.py`.
- Stable code/hint diagnostics contract validated in
  `tests/test_smonitor_catalog_contract.py`.
- Compatibility matrix documented in `devguide/compatibility_matrix.md`.

## 2. SMonitor 1.0 consumer-side alignment

Status note (2026-03-03):
- smonitor: pending
- depdigest: pending
- argdigest: in progress (consumer-side profile evidence pending collective closure)
- pyunitwizard: done locally (consumer side)
- collective validation: pending

PyUnitWizard local evidence:
- Profile contract (`user`, `dev`, `qa`, `agent`, `debug`) validated in
  `tests/test_smonitor_profiles_contract.py`.
- Catalog/code registry consistency and required hints validated in
  `tests/test_smonitor_catalog_contract.py`.
- Probe traceability event contract validated in
  `tests/test_introspection_probe.py`.

Open collective dependency:
- event-schema lock and bundle export stability must be closed in SMonitor core.

## 3. DepDigest 1.0 consumer-side alignment

Status note (2026-03-03):
- smonitor: pending
- depdigest: pending
- argdigest: done locally (consumer integration path validated in molsyssuite checklist)
- pyunitwizard: done locally (consumer side)
- collective validation: pending

PyUnitWizard local evidence:
- `_depdigest.py` hard/soft contract validated in
  `tests/test_depdigest_contract.py`.
- runtime contract introspection validated in
  `tests/integration/test_ecosystem_smoke.py`.

Open collective dependency:
- `depdigest audit` CLI collective leakage checks and import-cost budget are
  DepDigest-core closures.

## 4. ArgDigest 1.0 consumer-side alignment

Status note (2026-03-03):
- smonitor: in progress
- depdigest: in progress
- argdigest: done locally (`0.9.0` RC; reference in molsyssuite checklist)
- pyunitwizard: done locally (consumer side)
- collective validation: pending

PyUnitWizard local evidence:
- unit validation pipeline integration and failure mapping exercised in
  `tests/integration/test_ecosystem_smoke.py`.
- backend-to-catalog exception translation in PyUnitWizard validated in
  `tests/test_exception_translation_contract.py`.

Open collective dependency:
- full E2E path (unit-layer failure -> argdigest contract error -> smonitor event
  -> depdigest remediation hint) remains collective.

## 5. PyUnitWizard 1.0 local requirements

Status note (2026-03-03):
- smonitor: pending
- depdigest: pending
- argdigest: in progress (consumer-side error mapping validated)
- pyunitwizard: done locally
- collective validation: pending

PyUnitWizard local evidence:
- Isolated kernel/context restoration contract:
  `tests/test_context.py`.
- Exception translation into catalog-backed hierarchy:
  `tests/test_exception_translation_contract.py`.
- Fundamental unit lock:
  `tests/test_kernel_contract.py`.
- Performance baseline tracking:
  `devguide/performance_baseline_0.20.x.json`,
  generated by `devtools/benchmarks/conversion_baseline.py`.

## 6. Collective finality criterion (not locally closable)

This remains `pending` from the PyUnitWizard repository perspective.

To close collectively, we still need cross-repo evidence for:
- all four libraries at `>= 1.0.0`,
- E2E propagation from PyUnitWizard error to ArgDigest + SMonitor + DepDigest hint,
- host-level `smonitor --check` validating all layers simultaneously.
